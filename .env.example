# Warframe Tracker password. Leave blank to disable authentication.
PASSWORD=

# Random string for HMAC session signing. Required when PASSWORD is set.
SESSION_SECRET=

# Directory for SQLite database and prebuild cache (default: ./data)
DATA_DIR=./data
